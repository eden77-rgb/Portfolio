import HeroContent from "./HeroContent"

const HeroSection = () => {
    return (
        <header id="hero">
            <nav>
            </nav>

            <HeroContent />
        </header>
    )
}

export default HeroSection